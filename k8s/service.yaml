apiVersion: v1
kind: Service
metadata:
  name: hello-flask-service
spec:
  type: NodePort
  selector:
    app: hello-flask
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080

